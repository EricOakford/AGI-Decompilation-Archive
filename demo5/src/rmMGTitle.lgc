#define aCapitalLetters o2#define aLowerCaseLetters   o3#define aMother o4#define aGoose  o5#define roomTimer   v240#define roomState   v241#define cuedIt  f240#define titleScript f241if (newDemoRoom)  {  show.pic();  display(22, 5, "(c) 1987  Sierra On-Line, Inc.");  roomTimer = 2;  set(titleScript);  sound(Sound151, mgSoundDone);  }--roomTimer;if (roomTimer == 1)  {  if (titleScript)    {    set(cuedIt);    }  }if (titleScript)  {  if (cuedIt)    {    reset(cuedIt);    ++roomState;    if (roomState == 1)      {      animate.obj(aCapitalLetters);      set.view(aCapitalLetters, vMGTitle);      set.loop(aCapitalLetters, 0);      set.cel(aCapitalLetters, 0);      ignore.horizon(aCapitalLetters);      position(aCapitalLetters, 5, 24);      draw(aCapitalLetters);      animate.obj(aLowerCaseLetters);      set.view(aLowerCaseLetters, vMGTitle);      set.loop(aLowerCaseLetters, 1);      set.cel(aLowerCaseLetters, 0);      ignore.horizon(aLowerCaseLetters);      position(aLowerCaseLetters, 104, 24);      draw(aLowerCaseLetters);      ignore.objs(aCapitalLetters);      ignore.objs(aLowerCaseLetters);      move.obj(aCapitalLetters, 50, 24, 1, cuedIt);      move.obj(aLowerCaseLetters, 59, 24, 1, mgScriptDone);      }    if (roomState == 2)      {      stop.update(aCapitalLetters);      stop.update(aLowerCaseLetters);      animate.obj(aGoose);      set.view(aGoose, vMotherGoose);      set.loop(aGoose, 3);      fix.loop(aGoose);      set.cel(aGoose, 0);      position(aGoose, 0, 141);      set.priority(aGoose, 6);      draw(aGoose);      ignore.objs(aGoose);      animate.obj(aMother);      set.view(aMother, vMotherGoose);      set.loop(aMother, 0);      set.cel(aMother, 0);      position(aMother, 3, 137);      draw(aMother);      work = 2;      cycle.time(aMother, work);      cycle.time(aGoose, work);      move.obj(aMother, 66, 137, 1, cuedIt);      move.obj(aGoose, 63, 141, 1, mgScriptDone);      }    if (roomState == 3)      {      set.view(aMother, vMotherGoose);      set.loop(aMother, 2);      fix.loop(aMother);      set.cel(aGoose, 0);      set.cel(aMother, 0);      move.obj(aMother, 76, 137, 1, cuedIt);      move.obj(aGoose, 73, 141, 1, mgScriptDone);      work = 4;      cycle.time(aMother, work);      cycle.time(aGoose, work);      }    if (roomState == 4)      {      add.to.pic(vMGTitle, 2, 0, 22, 67, 0, 0);      add.to.pic(vMGTitle, 3, 0, 88, 66, 0, 0);      roomTimer = 2;      }    if (roomState == 5)      {      set.view(aMother, vMotherGoose);      set.loop(aMother, 0);      fix.loop(aMother);      move.obj(aMother, 100, 137, 1, cuedIt);      move.obj(aGoose, 97, 141, 1, mgScriptDone);      work = 2;      cycle.time(aMother, work);      cycle.time(aGoose, work);      }    if (roomState == 6)      {      nextRoom = rmCharSelect;      }    }  }return();[ Messages#message 1 "(c) 1987  Sierra On-Line, Inc."